#include <iostream>
using namespace std;
int main(){
  int n, v, somav=0, old=0;
  cin >> n;
  for (size_t i = 0; i < n; i++) {
    cin >> v;
    if(v==old){
      somav+=old;
    } else {

    }
  }
  return 0;
}
